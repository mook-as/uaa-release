#!/bin/bash

# enable UAA into degraded/limitedFunctionality mode
# or for crude downtime, shutdown UAA

<% if p('release_level_backup') %>
# 1. update config to enable

# retstart the process

  /var/vcap/bosh/bin/monit stop uaa
  #TODO monit is async, we need to wait until above action is successful
  sleep 10
<% end %>